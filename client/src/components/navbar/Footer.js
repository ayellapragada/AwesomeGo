import React from 'react';

const Footer = (props) => {
  return (
    <div style={outerContainerStyle}></div>
  );
};

const outerContainerStyle = {
  backgroundColor: 'black',
  color: 'white',
};

export default Footer;

